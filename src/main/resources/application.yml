micronaut:
  application:
    name: datasahi-flow
  server:
    cors:
      enabled: true
    port: ${DATASAHI_PORT:8081}
    dual-protocol: false

datasahi.flow:
  config:
    paths: ${DATASAHI_CONFIG_PATHS:/tmp/datasahi/config/flow.json}
  work.dir: ${DATASAHI_WORK_DIR}